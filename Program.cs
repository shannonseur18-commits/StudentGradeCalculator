using System;

class Program
{
    static void Main(string[] args)
    {
        // The UI is handled by index.html
        // This just confirms the program is running
        Console.WriteLine("Student Grade Calculator is running.");
        Console.WriteLine("Open index.html in your browser to use the UI.");
    }
}
